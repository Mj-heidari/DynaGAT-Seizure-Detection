DynaGAT FINAL RELEASE

Final model:
- Dynamic Graph Learning
- GATv2 spatial attention
- Multi-scale temporal encoder
- Temporal Transformer attention

Final corrections:
1. Adaptive threshold selection:
   - Avoid extremely conservative thresholds.
   - Optimize sensitivity while controlling false alarms.

2. Temporal event aggregation:
   - Use consecutive prediction windows.
   - Reduce isolated false detections.

3. Class imbalance handling:
   - Preserve seizure importance.
   - Reduce bias toward negative windows.

4. Evaluation:
   - Patient-independent LOPO.
   - Multi-patient validation.
   - Report AUROC, AUPRC, sensitivity, FA/hour, latency, ECE.

Training:
- Epochs: 24
- CUDA GPU enabled

This package is the final experiment candidate.
